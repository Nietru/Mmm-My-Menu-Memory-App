
{{#if message}}
  <div class="alert alert-danger">{{message}}</div>
{{/if}}

<div class="d-flex flex-column align-items-center ">
  <div class="col-md-3">
    <h2>Login</h2>
    <form action="/login" method="POST" class="form custom-form shadow p-3 mb-4">
      <div class="form-group ">
        <label for="email-login" class="h4">email:</label>
        <input class="rounded" type="text" id="email-login" />
      </div>
      <div class="">
        <label for="password-login" class="h4">password:</label>
        <input class="rounded" type="password" id="password-login" />
      </div>
      <div class="form-group">
        <button class="btn btn-primary" type="submit">login</button>
      </div>
    </form>
  </div>

  <div>
    <h3> or </h3>
  </div>
